import React from 'react'
import { Layout } from 'antd'
import styles from './style.module.scss'

const { Footer } = Layout

const FooterPlayer: React.FC = () => {
  return <Footer className={styles.footer}>ss</Footer>
}

export default FooterPlayer
