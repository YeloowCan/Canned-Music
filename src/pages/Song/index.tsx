import { FC, useEffect } from 'react'
// import { accountInfo } from '@/apis/account'

const Song: FC = () => {
  useEffect(() => {
    // accountInfo()
  }, [])

  return <div>Song</div>
}

export default Song
