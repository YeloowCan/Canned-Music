import React from 'react'
import { ILoginResult } from '../apis/types/login'
import { SESSION_LOCAL_KEY } from './keys'

// export const LoginContext = React.createContext<LoginState>(initialLoginState)
